\documentclass{ximera}

\input{../../preamble.tex}

\author{Kenneth Berglund}

\begin{document}
Consider the polynomial $p(x) = x^4 + 2x^3 - 3x^2$. 

\begin{exercise}
A factored form of this polynomial is 
\begin{multipleChoice}
\choice{$x^2(x - 2)^2$.}
\choice{$x^2(x + 3)^2$.}
\choice[correct]{$x^2(x + 3)(x - 1)$.}
\choice{$x(x + 3)(x - 1)$.}
\choice{$x^2(x -2)(x - 1)$.}
\end{multipleChoice}

\begin{exercise}
$p$ has $\answer{3}$ roots.

\begin{exercise}
The roots of $p$ in increasing order are $\answer{-3}$, $\answer{0}$, and $\answer{1}$.

\begin{exercise}
The multiplicity of 3 as a root of $p$ is $\answer{1}$.

The multiplicity of 0 as a root of $p$ is $\answer{2}$. 

The multiplicity of 1 as a root of $p$ is $\answer{1}$. 

\begin{exercise}
The degree of $p$ is \wordChoice{\choice{odd}\choice[correct]{even}}.

The leading coeffficient of $p$ is \wordChoice{\choice[correct]{positive}\choice{negative}}.

As $x \to -\infty$, $p(x) \to$ \wordChoice{\choice[correct]{$\infty$}\choice{$-\infty$}}. As $x \to \infty$, $p(x) \to$ \wordChoice{\choice[correct]{$\infty$}\choice{$-\infty$}}.

\begin{exercise}
Choose the graph of $p(x)$ from the options below.

\begin{image}
\begin{tikzpicture}
    \begin{axis}[ymin=-7, ymax=7]
         \addplot[<->, domain=-4:3,samples=200]{x^2*(x + 3)*(x - 1)} node{};
	\node(label) at (axis cs:-6,6) {$A$};	
    \end{axis}
\end{tikzpicture}
\begin{tikzpicture}
    \begin{axis}[ymin=-7, ymax=7]
         \addplot[<->, domain=-4:5, samples=200]{x^2*(x - 3)*(x - 1)} node{};
	\node(label) at (axis cs:-6,6) {$B$};	
    \end{axis}
\end{tikzpicture}
\end{image} 

\begin{image}
\begin{tikzpicture}
    \begin{axis}[ymin=-7, ymax=7]
         \addplot[<->, domain=-4:3, samples=200]{-x^2*(x + 3)*(x - 1)} node{};
	\node(label) at (axis cs:-6, 6) {$C$};	
    \end{axis}
\end{tikzpicture}
\begin{tikzpicture}
    \begin{axis}[ymin=-7, ymax=7]
         \addplot[<->, domain=-4:3, samples=200]{-x^2*(x + 3)*(x +1)} node{};
	\node(label) at (axis cs:-6,6) {$D$};	
    \end{axis}
\end{tikzpicture}
\end{image} 

\begin{multipleChoice}
\choice[correct]{A}
\choice{B}
\choice{C}
\choice{D}
\end{multipleChoice}
\end{exercise}
\end{exercise}
\end{exercise}
\end{exercise}
\end{exercise}
\end{exercise}


\end{document}